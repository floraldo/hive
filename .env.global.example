# Hive Global System Secrets - Example Configuration
# Copy this file to .env.global and fill in your actual infrastructure secrets
# These are Hive infrastructure secrets that apps should not normally access

# Database connections (if used by Hive core)
DATABASE_URL=postgresql://user:password@localhost:5432/hive
REDIS_URL=redis://localhost:6379/0

# Hive system secrets
HIVE_SECRET_KEY=your-hive-secret-key-32-chars-min
HIVE_JWT_SECRET=your-jwt-secret-key-for-auth

# Docker registry credentials (if used)
DOCKER_REGISTRY_URL=your-registry.com
DOCKER_REGISTRY_USER=your-registry-user
DOCKER_REGISTRY_TOKEN=your-registry-token

# Production deployment secrets
SSH_PRIVATE_KEY_PATH=/path/to/ssh/private/key
DEPLOYMENT_TOKEN=your-deployment-automation-token
DEPLOYMENT_HOST=your-production-host.com

# System monitoring and logging
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project
DATADOG_API_KEY=your-datadog-api-key
LOG_AGGREGATION_ENDPOINT=https://your-log-service.com/api

# SSL/TLS certificates (if managed by Hive)
SSL_CERT_PATH=/path/to/ssl/cert.pem
SSL_KEY_PATH=/path/to/ssl/private.key

# NOTE: This file is for Hive infrastructure secrets only.
# All API keys for external services are in .env.shared
# App-specific settings go in apps/{app_name}/.env
# Never commit .env.global to git!