# Hive Platform Environment Configuration
# Copy this file to .env and customize for your environment
#
# IMPORTANT: Never commit .env to version control!
# The .env file contains sensitive credentials and local paths.

# ============================================================
# Project Structure
# ============================================================

# Root directory of the Hive project
# Used by: Configuration management, path resolution
# Local dev: Auto-detected from git root
# Docker: /app
# K8s: /app
HIVE_PROJECT_ROOT=/path/to/hive

# ============================================================
# Logging Configuration
# ============================================================

# Logging verbosity level
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Default: INFO
LOG_LEVEL=INFO

# Enable emoji in log output (optional)
# Options: true, false
# Default: false
HIVE_EMOJI=false

# ============================================================
# Database Configuration
# ============================================================

# EcoSystemiser database path
# Local dev: data/ecosystemiser.db
# Docker: /app/data/ecosystemiser.db
# K8s: Use persistent volume mount
ECOSYSTEMISER_DB_PATH=data/ecosystemiser.db

# ============================================================
# AI Service Configuration
# ============================================================

# OpenAI API key for AI features
# Get your key from: https://platform.openai.com/api-keys
# Required for: ai-planner, ai-reviewer, ai-deployer
OPENAI_API_KEY=sk-your-openai-api-key-here

# Exa API key for search features (optional)
# Get your key from: https://exa.ai
EXA_API_KEY=your-exa-api-key-here

# ============================================================
# Deployment Configuration
# ============================================================

# Remote deployment base directory
# Default: /home/deploy/apps
# Used by: hive-deployment package
BASE_REMOTE_APPS_DIR=/home/deploy/apps

# Nginx configuration directory
# Default: /etc/nginx/conf.d
NGINX_CONF_D_DIR=/etc/nginx/conf.d

# Systemd service directory
# Default: /etc/systemd/system
SYSTEMD_SERVICE_DIR=/etc/systemd/system

# Deployment server user
# Default: deploy
SERVER_USER=deploy

# Nginx user and group
# Default: www-data (Linux), nginx (Docker)
NGINX_USER_GROUP=www-data

# ============================================================
# Security Configuration
# ============================================================

# Master encryption key for sensitive data
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
# CRITICAL: Keep this secret and unique per environment!
HIVE_MASTER_KEY=generate-a-secure-random-key-here

# ============================================================
# External Services (Optional)
# ============================================================

# Base URL for external API integrations
# Used by: Service discovery, webhook callbacks
# Local dev: http://localhost:8000
# Production: https://api.yourdomain.com
BASE_URL=http://localhost:8000

# Claude Code binary path (for integration testing)
# Default: Auto-detected from PATH
# Override if using custom installation
# CLAUDE_BIN=/usr/local/bin/claude

# ============================================================
# Development Environment Notes
# ============================================================
#
# Quick Start (Local Development):
# 1. cp .env.example .env
# 2. Set OPENAI_API_KEY (required for AI features)
# 3. Customize paths if not using default project structure
# 4. Run: conda activate hive && poetry install
#
# Docker Deployment:
# 1. Mount .env as secrets volume
# 2. Override HIVE_PROJECT_ROOT=/app
# 3. Override database paths to use volumes
# 4. Set POETRY_VIRTUALENVS_CREATE=false
#
# Kubernetes Deployment:
# 1. Create ConfigMap for non-sensitive values
# 2. Create Secret for API keys and master key
# 3. Mount as environment variables
# 4. Use persistent volumes for databases
#
# ============================================================
# Security Best Practices
# ============================================================
#
# - NEVER commit .env to version control
# - Use different keys per environment (dev/staging/prod)
# - Rotate HIVE_MASTER_KEY periodically
# - Store production secrets in secure vaults (AWS Secrets Manager, etc.)
# - Use read-only file permissions: chmod 600 .env
# - Audit .env access in production environments
#
# ============================================================
