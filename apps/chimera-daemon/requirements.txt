# Chimera Daemon Dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
aiosqlite>=0.19.0
httpx>=0.25.0

# Development dependencies
pytest>=8.3.2
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
ruff>=0.1.15
mypy>=1.8.0

# Local packages (install from parent directories)
# These should be installed separately:
# pip install -e ../../packages/hive-logging
# pip install -e ../../packages/hive-config
# pip install -e ../../packages/hive-orchestration
# pip install -e ../../packages/hive-db
